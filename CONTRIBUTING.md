<div dir="rtl" align="right">

# 🤝 השתתפות ביצירה

## עקרונות

1. שומרים על הכוונה לפני הסגנון.
2. לא משנים מילים מקודשות בלי תיעוד ובדיקה.
3. כל שינוי מהותי בשיר מסביר מה השתפר ולמה.
4. לא מוחקים קרדיט קודם; מוסיפים את תרומת הגרסה.
5. קבצי אודיו כבדים נשמרים מחוץ להיסטוריית Git או באמצעות Git LFS.

## ענפים

- `main` — גרסאות יציבות.
- `develop` — שילוב עבודה פעילה.
- `track/SC-XX-slug` — שיר יחיד.
- `release/vX.Y.Z` — הכנת גרסה.

## הודעות Commit

```text
lyrics(SC-01): sharpen chorus and practical punch
sources(SC-01): add Psalm reference
production(SC-01): define 78 BPM arrangement
visual(SC-01): add cover direction
catalog: advance SC-01 to draft
```

## Pull Request

לפני מיזוג:

- הרץ `python3 scripts/validate_repo.py`.
- מלא את רשימת 12 שערי האיכות.
- ציין אילו קבצים ומקטעים השתנו.
- וודא שכל שותף קיבל קרדיט מדויק.

</div>
